declare type ActualCpfGeneratorOptions = {
    format: boolean;
    prefix: string;
};
declare type CpfGeneratorOptions = Partial<ActualCpfGeneratorOptions>;

/**
 * Generate a valid CPF (Brazilian ID document) numeric sequence.
 */
declare function cpfGen(options?: CpfGeneratorOptions): string;

export default cpfGen;
